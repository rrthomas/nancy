Package as CLI plus two expander modules, plus base module.

Look at Leo @clean

Add support for patches with diff. This is an alternative to Nancy syntax for files that can't be edited, and must be used as pristine sources.

Use this for "Recalled Home", using the multiple input directories to search the poetry archive (which is in a submodule, i.e. subdirectory).
